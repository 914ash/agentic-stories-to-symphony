# Specs

Repo-local spec revisions are the canonical planning source of truth for this workspace.

Generated spec revisions, projection maps, and resumable sessions are intentionally kept out of the public repo. They are created locally during intake runs and should remain local operator artifacts rather than checked-in examples.
